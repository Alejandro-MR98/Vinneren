<p style="color: green"><%= notice %></p>

<%= render @sub_category %>

<div>
  <%= link_to "Editar sub categoría", edit_sub_category_path(@sub_category), class: "btn btn-primary" %> |
  <%= link_to "Regresar a sub categorías", sub_categories_path , class: "btn btn-primary"%>
  <br>
  <br>
  <%= button_to "Elimanr sub categoría", @sub_category, method: :delete, class: "btn btn-danger" %>
</div>
